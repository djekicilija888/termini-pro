services:
  - type: web
    name: termini-platforma
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PLATFORM_NAME
        value: Termini Platforma
      - key: SUPERADMIN_NAME
        value: Super Admin
      - key: SUPERADMIN_EMAIL
        value: admin@platform.local
      - key: SUPERADMIN_PASSWORD
        sync: false
